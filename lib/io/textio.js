'use strict'

const fs = require( 'fs' )

const getText = ( filename, callback ) => {}

const saveText = ( filename, txt ) => {}

const getJSON = ( filename, callback ) => {}

const saveJSON = ( filename, obj ) => {}

module.exports = {
	getText, saveText, getJSON, saveJSON
}
